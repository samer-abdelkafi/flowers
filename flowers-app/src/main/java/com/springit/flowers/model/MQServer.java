package com.springit.flowers.model;

public class MQServer extends Server {

    MQServer(String name, ServerType type, String host, String user, String password) {
        super(name, type, host, user, password);
    }
}
