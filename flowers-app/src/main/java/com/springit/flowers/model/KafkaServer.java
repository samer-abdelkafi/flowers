package com.springit.flowers.model;

public class KafkaServer extends Server {

    KafkaServer(String name, ServerType type, String host, String user, String password) {
        super(name, type, host, user, password);
    }
}
